{

  "manifest_version": 2,
  "name": "Volume Control Everywhere",
  "version": "1.0",

  "description": "Modern websites seem to hate to give you control over audio and video volume, so this is apparently needed. This addon will detect HTML5 audio/video components in the current tab and provide a quick way to change the volume. Note that not every sound coming out of your browser is a HTML5 audio or video element, so this addon might not solve all your problems.",

  "icons": {
    "48": "icons/icon.svg",
    "96": "icons/icon.svg"
  },

  "permissions": [
    "activeTab"
  ],

  "optional_permissions": [
    "<all_urls>"
  ],

  "browser_action": {
    "default_icon": "icons/icon.svg",
    "default_title": "Volume Control Everywhere",
    "default_popup": "popup/control.html",
    "theme_icons": [
      {
        "dark": "icons/icon.svg",
        "light": "icons/icon_dark.svg",
        "size": 32
      }
    ],
    "browser_style": true
  },

  "options_ui": {
    "page": "settings/settings.html"
  }
}